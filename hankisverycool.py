def example(var1 var2):
  self.move("forward", new Duration(driveTime))
  self.move(90, left)
  for _ in range(9):
    self.move(10, left)
  self.move("forward", new Duration(driveTime))
  Failed to parse: python:
  notUsed = 10
    Failed to parse: notUsed = 10
    name = robot.getName()
    Failed to parse: name = robot.getName()
howLong = 10
nameRobot("Our Robot")
example(howLong)
